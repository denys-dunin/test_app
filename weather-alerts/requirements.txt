# Runtime deps are zero — the script uses only the Python stdlib.
# The single optional dependency below enables cross-platform desktop
# notifications (macOS, Linux, Windows). Skip it if you only need stdout.
plyer>=2.1.0
